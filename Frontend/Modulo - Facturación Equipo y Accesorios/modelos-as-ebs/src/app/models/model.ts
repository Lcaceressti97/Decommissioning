/**
 * Modelo principal que se muestra en la tabla de la pantalla
 * y el cual se realizará el crud de la misma
 * 
 */
export interface ModelAsEbsModel {
    id?:number;
    codMod?:string;
    codEbs?: string;
    subBod?: string;
    newMod?:string;
    name?: string;
}